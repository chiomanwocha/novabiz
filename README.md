# NovaBiz Merchant Dashboard

FirstBank NovaPay's small-merchant dashboard — a take-home assessment build. See `CLAUDE.md` for the full brief and `BUILD_LOG.md` for build status.

This README is a placeholder until CP-25, when it gets filled in with setup, architecture, and trade-off documentation per the checklist in `CLAUDE.md` section 9.

## Quick start

```bash
npm install
npm run dev
```
